# Ensure shell scripts always have LF line endings
*.sh text eol=lf

# Cron/logrotate files are parsed by Linux tooling and must not contain CRLF.
infrastructure/vm-cron/crontab text eol=lf
*.conf text eol=lf
